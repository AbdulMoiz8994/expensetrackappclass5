import React from 'react'

export const Transaction = ({transcation}) => {
    return (
        <div>
            {/* <li className="lilist"> */}
          <button  className="lilist">X</button>
            {/* </li> */}
        </div>
    )
}
